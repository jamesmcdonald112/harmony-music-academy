export async function deliverInfoPack(input: {
	fullName: string;
	email: string;
	phone: string;
	studentAge?: number;
	message?: string;
}): Promise<void> {
	// TODO: Implement email sending (Resend, SMTP, etc.)
	void input;
}
